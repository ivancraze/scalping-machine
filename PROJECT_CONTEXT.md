# Контекст проекта для продолжения работы с ИИ

Этот файл нужен, чтобы открыть проект на другом компьютере или в новом чате и быстро дать ИИ корректный контекст.

## Коротко

**Pulse Terminal** — личный крипто-скринер для наблюдения за рынком. Он не торгует, не выставляет ордера и не должен хранить торговые ключи в браузере.

Текущий стек: Vite, React, TypeScript, SCSS Modules, Lightweight Charts и набор `difurious/lightweight-charts-line-tools-*`.

Текущий источник данных — публичный **Binance Spot REST API**. В интерфейсе пока встречается надпись «Binance Futures» — это известное несоответствие, которое нельзя закреплять в новой логике.

## Как начать на новом компьютере

1. Склонировать или скопировать весь проект вместе с `package.json`, `package-lock.json` и markdown-документацией.
2. Установить Node.js 20+.
3. В папке проекта выполнить:

   ```powershell
   npm install
   npm run dev
   ```

4. Перед первой правкой нового агента попросить его прочитать в таком порядке:

   1. `AGENTS.md`;
   2. этот файл (`PROJECT_CONTEXT.md`);
   3. `PROJECT_GOALS.md`;
   4. `TODO.md`;
   5. `README.md`.

5. После кодовых изменений запускать:

   ```powershell
   npm run check
   ```

## Готовый стартовый промпт для нового чата

Скопируй в новый чат вместе с открытой папкой проекта:

```text
Работаем над проектом Pulse Terminal. Это личный крипто-скринер только для наблюдения, без исполнения сделок и без торговых ключей во фронтенде.

Перед началом прочитай AGENTS.md, PROJECT_CONTEXT.md, PROJECT_GOALS.md, TODO.md и README.md. Соблюдай FSD, SCSS Modules и существующие настройки ESLint/Prettier/Stylelint. Не создавай папки-заглушки: при FSD-миграции переноси реальные реализации. После изменений запускай npm run check.

Сначала кратко опиши, как ты понял текущее состояние, затем выполни задачу: <вставь конкретную задачу>.
```

## Текущее состояние интерфейса

- брендовый блок `PULSE` и единственная вкладка «Список наблюдения»;
- большой свечной график слева и список монет справа;
- поиск и сортировка списка по названию, объёму, изменению цены, NATR и корреляции с BTC;
- переключатели таймфреймов в строке состояния графика;
- объём в нижней части графика;
- левая панель быстрых объектов: горизонтальный уровень, трендовая линия, горизонтальный луч, прямоугольник;
- раскрытое меню остальных инструментов line-tools;
- правый клик удаляет выбранный объект;
- SCSS Modules находятся рядом со страницей и виджетом графика.

## Особенности и известные ограничения

### Данные

- Рынок обновляется REST-поллингом раз в 30 секунд.
- Свечи загружаются из публичного Binance Spot REST API с лимитом 72 бара.
- Интервалы 1с, 5с и 15с пока фактически отображаются как 1м: реальная поддержка потребует trade stream, агрегации и хранилища.
- Корреляция с BTC вычисляется в браузере и может создавать серию запросов. Нужны кэширование, отмена запросов и ограничение параллельности.

### График

- Объекты создаются нативным `line-tools` и привязаны к времени/цене.
- Пока они не сохраняются между перезагрузками. Это задача №1 из `TODO.md`.
- Пока подключены все пакеты инструментов сразу, поэтому production bundle примерно 530 kB minified. В будущем нужен dynamic import.
- При обновлении свечей используется `setData()` и `fitContent()`; для real-time следует перейти на `series.update()` и не сбрасывать пользовательский масштаб.
- Меню дополнительных инструментов располагается у левой панели. Пока оно открыто, canvas отключает pointer events, чтобы график не перехватывал выбор в меню.

### Архитектура

FSD-миграция начата, но не закончена.

```text
src/
  app/App.tsx
  pages/market-terminal/ui/MarketTerminalPage.tsx
  widgets/chart-workspace/ui/ChartCanvas.tsx
  entities/market/api/binance.ts
  entities/market/model/types.ts
  shared/
```

Следующие хорошие кандидаты на вынос из страницы: market list, сортировки, поиск, выбор символа, выбор таймфрейма, панель инструментов.

## Что нельзя сломать

- Не превращать объекты графика в отдельный SVG-overlay: они должны оставаться line-tools.
- Не добавлять торговлю или хранение трейдинговых ключей.
- Не смешивать Spot и Futures без явного разделения.
- Не выносить секреты Telegram или бирж во фронтенд.
- Не копировать код, брендинг или ассеты чужих скринеров.
- Не удалять или переписывать чужие изменения без явного запроса.

## Документы проекта

| Файл                     | Для чего нужен                               |
| ------------------------ | -------------------------------------------- |
| `AGENTS.md`              | обязательные правила для ИИ-агентов          |
| `PROJECT_CONTEXT.md`     | переносимый контекст и стартовый промпт      |
| `PROJECT_GOALS.md`       | глобальные продуктовые и технические цели    |
| `TODO.md`                | ближайший упорядоченный бэклог               |
| `RECOMMENDATIONS.md`     | стратегия развития, агенты и лучшие практики |
| `README.md`              | фактическое описание, запуск и команды       |
| `.agents/code-review.md` | инструкция performance code reviewer         |

## Как вести контекст дальше

После каждой крупной завершённой функции обновлять этот файл:

- что появилось для пользователя;
- где лежит реализация;
- какие источники данных используются;
- известные ограничения, решения и принятые компромиссы;
- что обязательно проверить следующему агенту.

Так новый чат получает актуальную карту проекта, а не вынужден угадывать историю по коду.
